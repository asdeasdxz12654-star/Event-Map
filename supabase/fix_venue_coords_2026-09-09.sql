-- 좌표 검증 스크립트(verify-venue-coords.mjs --fix)가 잘못 교체한 좌표 정정.
-- Supabase 대시보드 > SQL Editor 에서 실행하세요. 여러 번 실행해도 안전합니다.
--
-- 무슨 일이 있었나
--   지역검색 결과 채점이 "같은 시·도면 가점"만으로도 채택되도록 느슨해서, 이름이 전혀
--   다른 장소가 통과했다. 그 결과 두 행사의 좌표가 아래처럼 엉뚱한 곳으로 바뀌었다.
--     호요랜드2026    : 킨텍스 제2전시장 -> 스타필드 바운스(트램펄린장), 약 1.5km 벗어남
--     명조 2주년 페스티벌 : 킨텍스        -> 일산동구청,               약 2km 벗어남
--   채점 로직은 상호명 또는 도로명 주소가 일치해야만 채택하도록 고쳤다(naver-local.mjs).
--   이미 반영된 값은 코드로 되돌릴 수 없어서 여기서 직접 정정한다.
--
-- 어떤 값으로 되돌리나
--   crawler/src/known-events.mjs가 KINTEX 좌표로 쓰고 있는 값(37.669119, 126.7460896).
--   두 행사 모두 KINTEX에서 열린다. 호요랜드는 제2전시장이라 이 좌표(제1전시장 기준)와
--   700m 정도 차이가 나지만, 같은 단지 안이라 지도에서 찾는 데는 문제가 없고 이전에
--   박혀 있던 트램펄린장·구청보다 정확하다. 정확한 홀 위치가 필요하면 관리자 화면에서
--   위도·경도를 직접 수정하면 된다(그 뒤로는 크롤러가 덮어쓰지 않는다).

update public.events
set venue_lat = 37.669119,
    venue_lng = 126.7460896
where id in (
  'nd-20260903-7ae385', -- 호요랜드2026 (일산 킨텍스 제2전시장 7, 8홀)
  'nd-20260903-9724c8'  -- 명조 2주년 페스티벌 (킨텍스)
);

-- 확인용: 실행 후 아래로 결과를 볼 수 있다.
-- select id, title, venue, venue_lat, venue_lng from public.events
-- where id in ('nd-20260903-7ae385', 'nd-20260903-9724c8');
